#<IfModule mod_rewrite.c>
 #  RewriteEngine On
  # RewriteCond %{REQUEST_FILENAME} !-d
  # RewriteCond %{REQUEST_FILENAME} !-f
  # RewriteCond %{REQUEST_FILENAME}.php -f
  # RewriteRule ^([a-zA-Z0-9-_]+)$ $1.php [QSA,L]
#</IfModule>

#<IfModule mod_alias.c>
 #   RedirectMatch 404 /wd.is
#</IfModule>
